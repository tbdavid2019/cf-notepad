{
  "name": "888wiki",
  "version": "1.0.0",
  "author": "DAVID888 <tbdavid2019>",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "wrangler": "^4.134.0"
  },
  "private": true,
  "cloudflare": {
    "bindings": {
      "SCN_SALT": {
        "description": "Generate a unique random string for password hashing (for example, `openssl rand -hex 32`)."
      },
      "SCN_SECRET": {
        "description": "Generate a unique random secret for signing sessions and tokens (for example, `openssl rand -hex 32`)."
      },
      "SCN_ADMIN_PW": {
        "description": "Choose a strong password for the admin dashboard. The admin route is configured separately with SCN_ADMIN_PATH."
      },
      "GROQ_API_KEY": {
        "description": "Optional. Add a Groq API key to enable hosted AI transcription and writing features."
      }
    }
  },
  "scripts": {
    "build:block-editor": "node scripts/build-block-editor.mjs",
    "build:canvas-editor": "node scripts/build-canvas-editor.mjs",
    "build:whiteboard-editor": "node scripts/build-whiteboard-editor.mjs",
    "build:ocr": "node scripts/build-ocr-client.mjs",
    "prepare:skill-doc": "node scripts/generate-agent-skill.mjs",
    "prestart": "npm run prepare:skill-doc",
    "start": "wrangler dev",
    "predev": "npm run prepare:skill-doc",
    "dev": "wrangler dev",
    "predeploy": "npm run prepare:skill-doc && npm run build:block-editor && npm run build:canvas-editor && npm run build:whiteboard-editor && npm run build:ocr",
    "deploy": "npm run db:init && wrangler deploy",
    "db:init": "wrangler d1 execute NOTE_HISTORY_DB --remote --file=./schema/note_history.sql && wrangler d1 execute NOTE_HISTORY_DB --remote --file=./schema/note_stats.sql && wrangler d1 execute NOTE_HISTORY_DB --remote --file=./schema/note_annotations.sql",
    "prepublish": "npm run prepare:skill-doc",
    "publish": "wrangler deploy",
    "pretest": "npm run prepare:skill-doc && npm run build:block-editor && npm run build:canvas-editor && npm run build:whiteboard-editor && npm run build:ocr",
    "test": "node --test test/**/*.test.mjs"
  },
  "dependencies": {
    "@blocknote/core": "^0.54.2",
    "@blocknote/mantine": "^0.54.2",
    "@blocknote/react": "^0.54.2",
    "@excalidraw/excalidraw": "^0.18.1",
    "@firecrawl/anydoc-wasm": "^0.1.6",
    "@mantine/core": "^9.5.1",
    "@mantine/hooks": "^9.5.1",
    "@mantine/utils": "^6.0.22",
    "@paddleocr/paddleocr-js": "^0.4.2",
    "@takumi-rs/helpers": "^2.12.0",
    "@tsndr/cloudflare-worker-jwt": "^1.1.7",
    "@xyflow/react": "^12.11.6",
    "cookie": "^2.0.1",
    "dayjs": "^1.11.2",
    "html-to-image": "^1.11.13",
    "itty-router": "^2.6.1",
    "jsdom": "^29.1.1",
    "lucide-react": "^1.47.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "takumi-pdf": "^0.11.3",
    "zustand": "^4.5.7"
  },
  "overrides": {
    "@tiptap/core": "3.31.3",
    "@tiptap/extension-bold": "3.31.3",
    "@tiptap/extension-code": "3.31.3",
    "@tiptap/extension-italic": "3.31.3",
    "@tiptap/extension-strike": "3.31.3",
    "@tiptap/extension-text": "3.31.3",
    "@tiptap/extension-underline": "3.31.3",
    "@tiptap/extensions": "3.31.3"
  }
}
